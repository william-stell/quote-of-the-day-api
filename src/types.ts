/**
 * Represents a quote with its text content and the author's name.
 */
export interface Quote {
  /** The actual quote text */
  text: string;

  /** The name of the person who said or wrote the quote */
  author: string;
}
